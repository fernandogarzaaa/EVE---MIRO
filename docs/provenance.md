# Provenance

Four kinds, never mixed:

| Kind | Meaning |
|---|---|
| OBSERVED | Public measurement / published observation |
| DERIVED | Folded from events (e.g. WorldState) |
| FORECAST | Model forecast issued at cutoff (e.g. Open-Meteo forecast endpoint) |
| SIMULATED | Generated by a simulation engine |

Ingesting `SIMULATED` on the observed channel raises `ProvenanceError`.
Tests fail if this is allowed.

The provenance graph supports “why did the simulator predict this?” by walking
`conclusion → experience → state → event → source`.
